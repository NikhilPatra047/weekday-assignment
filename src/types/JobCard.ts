export type JOB_CARD = {
  jobTitle: string, 
  jobRole: string,
  companyName: string,
  location: string,
  jobDescription: string, 
  expRequired: number | null,
  logo: string,
  link: string,
  minPay: number | null, 
  maxPay: number | null,
}